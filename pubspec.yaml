name: deutsch_garden
description: Fully offline A1-C2 German learning app for Android, Windows, macOS, iOS and Linux, with an on-device speaking tutor, story mode, SM-2 review, practice games and exam preparation.
publish_to: 'none'
version: 3.2.0+5

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.5
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Used only by the widget smoke test, to install an in-memory backing store
  # for SharedPreferencesAsync.
  shared_preferences_platform_interface: ^2.4.0

flutter:
  uses-material-design: true
